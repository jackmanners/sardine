% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completion-reports.R
\name{get_form_completion_status}
\alias{get_form_completion_status}
\title{Get Form Completion Status}
\usage{
get_form_completion_status(
  project,
  forms,
  event = NULL,
  status = c("complete", "incomplete", "not_started", "unverified")
)
}
\arguments{
\item{project}{A redcap_project object}

\item{forms}{Character vector of form names to check}

\item{event}{Character. Event name for longitudinal projects (default: NULL)}

\item{status}{Character vector. Which statuses to include: "complete", "incomplete",
"not_started", "unverified". Default is all statuses.}
}
\value{
A data frame with participant IDs and their completion status for each form
}
\description{
Gets the completion status for specific forms across all participants,
useful for identifying which participants need follow-up.
}
\examples{
\dontrun{
project <- redcap_project()

# Get all completion statuses
completion <- get_form_completion_status(
  project,
  forms = c("demographics", "baseline_survey")
)

# Only incomplete forms (for follow-up)
incomplete <- get_form_completion_status(
  project,
  forms = c("follow_up_1", "follow_up_2"),
  status = c("incomplete", "not_started")
)
}
}
